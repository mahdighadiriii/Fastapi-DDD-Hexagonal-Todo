services:
  postgres:
    image: postgres:16.3-alpine
    container_name: postgres
    ports:
      - "5432:5432"
    env_file:
      - secrets/.env
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data: